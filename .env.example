# Optional shared server credentials. BOTH must be set to enable shared access.
OPENAI_API_KEY=
RESEARCH_PASSWORD=
# Comma-separated allowed OpenAI model IDs for shared credentials.
OPENAI_MODELS=gpt-4.1-mini,gpt-4.1
